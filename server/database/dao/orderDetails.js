const create = (body) => {
  return null; //OrderDetails.create(body);
};

const remove = (orderId) => {
  return null; // OrderDetails.destroy({ where: { orderId } });
};

module.exports = {
  create,
  remove,
};
